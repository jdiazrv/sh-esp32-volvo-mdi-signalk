# Name,   Type, SubType, Offset,   Size,     Flags
# 4 MiB flash. Two OTA slots of 1,6875 MiB leave working margin over a build
# that had reached 97,6 % of the previous 1,5 MiB slots. SPIFFS keeps 512 KiB,
# which is ample for two 128 KiB capture generations plus the SensESP
# configuration files, and leaves the free blocks SPIFFS needs to garbage
# collect. NVS keeps its address, so the learned CAN bitrate and the generated
# AP password survive; SPIFFS moves, so the saved WiFi/Signal K configuration
# must be entered once after flashing this table over USB.
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x1B0000,
app1,     app,  ota_1,   0x1C0000, 0x1B0000,
spiffs,   data, spiffs,  0x370000, 0x80000,
coredump, data, coredump,0x3F0000, 0x10000,
